<h1 class="text-center" style="margin: 30px 0px 0px 20px "> You want to see all the AI tools in a single category ? Then
  just click them!! </h1>
<div class="d-flex flex-wrap justify-content-center" style="list-style-type: none; padding-left: 0;">
  <% @categories.each do |category| %>
    <div class="m-2">
      <%= link_to category.category, category_page_path(category.id), class: "btn btn-light", style: "width: 250px; padding: 10px 20px; margin: 30px 20px 0px 0px; box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.1); transition: transform 0.3s ease;", onmouseover: "this.style.transform='translateY(-5px)';", onmouseout: "this.style.transform='none';" %>
    </div>
  <% end %>
</div>

  <br>
  <div class="col-lg-2 text-center">
    <a class="btn btn-primary" style="margin-bottom: 20px" href="<%= root_path %>" role="button">Go back to Home
      page</a>
  </div>